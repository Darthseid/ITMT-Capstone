f = open("example.json", "r") #This is a test to see if the Python code will actually read and write to the file.
print(f.read()) #This prints the contents of the file so the coder knows what is inside of it
